#pragma once

#include "../common/common.h"

void PrepareTestData(vector<TD_S_S_I>& testData);

int compareVersion(string version1, string version2);

int compareVersion_r(string version1, string version2);
